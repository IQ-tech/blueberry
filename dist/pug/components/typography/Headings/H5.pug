mixin H5({type})
	- const baseClass = 'iq-h5'
	- const typeClass = !!type ? `${baseClass}--${type}` : `${baseClass}--primary`
	- const defaultTitle = 'Heading Five'
	h5(
		class=baseClass
		class=typeClass
	)
		if block
			block
		else
			span #{defaultTitle}
