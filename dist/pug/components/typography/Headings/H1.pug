mixin H1({type})
	- const baseClass = 'iq-h1'
	- const typeClass = !!type ? `${baseClass}--${type}` : `${baseClass}--primary`
	- const defaultTitle = 'Heading One'
	h1(
		class=baseClass
		class=typeClass
	)
		if block
			block
		else
			span #{defaultTitle}
