include ../Button/index.pug
include ../Button/ButtonIcon.pug


mixin IconButton({spaceBetween, type, size, color, loading, expand, text, disabled, onlyIcon, iconRight})
	+Button({spaceBetween, type, size, color, loading, expand, text, disabled, onlyIcon, iconRight})&attributes( attributes )
		+ButtonIcon()
			if block
				block
