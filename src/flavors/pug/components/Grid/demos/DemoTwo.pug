include ../index.pug
include ../Row.pug
include ../SpanColumns.pug

mixin GridDemo()
	+Grid({prototyping: true})
		+GridRow({})
			+GridSpanColumns({phone: "full", tablet: 4, desktop: 2})
				.demo-content col

			+GridSpanColumns({phone: "half", tablet: 4, desktop: 10})
				.demo-content col

