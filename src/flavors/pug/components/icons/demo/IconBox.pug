include ../../FlexGrid/SpanColumns.pug

mixin IconBox({filename})
	+FlexGridSpanColumns({})
		.icon-box(
			title=filename
		)
			.icon-box__icon-holder
				if block
					block
			p.icon-box__name #{filename}


