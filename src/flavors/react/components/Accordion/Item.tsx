import * as React from "react";
import classNames from "classnames"


interface AccordionItemProps {
  /** if true, the panel starts opened */
  startOpen: boolean;
}

const Item: React.FC = () => {
  return <li>item</li>;
};

export default Item;
